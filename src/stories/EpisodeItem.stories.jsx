import { EpisodeItem } from "../components/presentational/EpisodeItem";

export default {
  title: "EpisodeItem",
  component: EpisodeItem,
};

export const Default = {
    args: {
        episode: {
            episode: "S01E01",
            name: "Pilot",
        }
    }
}

// Не получается(((((((((((((((